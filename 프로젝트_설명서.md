# KABS: Keyframe-based Action-oriented Behavioral Signatures

## 프로젝트 개요

### 1. 프로젝트 목적

KABS는 비디오에서 **중요한 순간을 자동으로 추출하는 지능형 키프레임 추출 시스템**입니다. 특히 다중 객체(예: 행동 연구에서의 쥐들) 추적 및 분석을 위해 설계되었으며, 객체의 수, 위치, 상호작용 등 중요한 구성 변화를 포착하는 가장 정보가 풍부한 프레임을 추출합니다.

**핵심 목표:**
- 비디오에서 객체 구성이 크게 변화하는 중요한 순간을 자동 식별
- 중복성을 최소화하면서 정보량을 최대화
- 비디오 요약 및 행동 분석의 효율성 향상

### 2. 주요 특징

- ✅ **다단계 파이프라인**: 객체 감지 → 추적 → 프로파일 필터링 → 재식별 → 탐욕적 선택
- ✅ **고급 객체 추적**: YOLO11/RT-DETR + ByteTrack/BOT-SORT
- ✅ **시각적 중복 제거**: 히스토그램 기반 프로파일 추적
- ✅ **객체 재식별**: OSNet 딥러닝 모델을 활용한 객체 통합
- ✅ **최적화된 커버리지**: 탐욕 알고리즘으로 최소 프레임으로 최대 정보 커버
- ✅ **포괄적인 평가 프레임워크**: Ground Truth 비교 및 다양한 메트릭

---

## 전체 동작 과정 및 알고리즘

### 파이프라인 개요

KABS는 **5단계 파이프라인**으로 구성됩니다:

```
비디오 입력
    ↓
[단계 0] 모델 로딩
    ↓
[단계 1] 1차 선택 (Primary Selection)
    ↓
[단계 2] 프로파일 추적 (Pre-Greedy Filtering)
    ↓
[단계 3] 재식별 및 프레임 병합 (Re-ID & Frame Merging)
    ↓
[단계 4] 탐욕적 커버리지 선택 (Greedy Coverage Selection)
    ↓
[단계 4.5] 후처리 프로파일 추적 (Post-Greedy Filtering) [선택사항]
    ↓
[단계 5] 결과 저장 및 출력
    ↓
키프레임 출력
```

---

### 단계별 상세 설명

#### **단계 0: 모델 로딩 및 초기화**

**목적**: 파이프라인에 필요한 모든 딥러닝 모델과 알고리즘 로드

**사용 모델:**
1. **객체 감지 (Object Detection)**
   - YOLO11 (You Only Look Once v11)
   - RT-DETR (Real-Time Detection Transformer)
   - 역할: 각 프레임에서 객체의 위치(바운딩 박스) 감지

2. **객체 추적 (Object Tracking)**
   - ByteTrack: 경량화된 다중 객체 추적
   - BOT-SORT: ByteTrack + Re-ID를 결합한 고급 추적
   - 역할: 프레임 간 객체의 연속성 유지 (ID 할당)

3. **재식별 (Re-Identification)**
   - TorchReID OSNet (x1_0, 512-dim features)
   - 역할: 다른 프레임의 동일 객체를 외형 기반으로 매칭

4. **깊이 추정 (Depth Estimation)** [선택사항]
   - DPT (Dense Prediction Transformer)
   - 역할: 3D 정보 추출 (필요 시)

**출력**: 초기화된 모델 객체들

---

#### **단계 1: 1차 선택 (Primary Selection)**

**목적**: 객체 구성이 변화하는 잠재적 키프레임 후보 식별

**알고리즘:**
```
1. 모든 프레임을 순차적으로 처리
2. 각 프레임에서:
   - 객체 감지 실행
   - 추적 ID 할당 (ByteTrack/BOT-SORT)
3. 현재 프레임의 추적 ID 집합 = {id1, id2, id3, ...}
4. 이전 프레임과 ID 집합 비교:
   - 집합이 동일하면: 같은 그룹으로 분류
   - 집합이 다르면: 새로운 그룹 시작
5. 각 그룹의 중간 프레임을 키프레임 후보로 선택
```

**예시:**
```
프레임 1-10:   추적 ID = {1, 2, 3}      → 그룹 A
프레임 11-25:  추적 ID = {1, 2, 3, 4}   → 그룹 B (새 객체 4 등장!)
프레임 26-40:  추적 ID = {1, 2, 4}      → 그룹 C (객체 3 사라짐!)
프레임 41-60:  추적 ID = {1, 2, 4, 5}   → 그룹 D (새 객체 5 등장!)

선택된 키프레임 후보: [5, 18, 33, 50] (각 그룹의 중간 프레임)
```

**출력**: 초기 키프레임 후보 리스트 (높은 recall, 일부 중복 포함 가능)

**주요 파라미터:**
- `detector`: 'YOLO' 또는 'RTDETR'
- `tracker`: 'ByteTrack' 또는 'BOTSORT'

---

#### **단계 2: 프로파일 추적 (Pre-Greedy Filtering)**

**목적**: 시각적으로 유사한 중복 프레임 제거 (precision 향상)

**알고리즘: 결합 히스토그램 비교**

```
입력: 키프레임 후보 리스트 F = [f1, f2, ..., fn]
출력: 필터링된 리스트 F'

반복 횟수(iterations) 동안:
    각 프레임 fi에 대해:
        1. 결합 히스토그램 계산:
           - HSV 색공간으로 변환
           - Brightness(V) 히스토그램 × 50%
           - Saturation(S) 히스토그램 × 50%
           - 정규화된 결합 히스토그램 생성

        2. 양방향 윈도우 탐색:
           - 윈도우 범위: [i - window_size/2, i + window_size/2]
           - 윈도우 내 모든 프레임 fj와 비교

        3. 유사도 계산:
           - Bhattacharyya Distance 사용
           - distance = cv2.compareHist(hist_i, hist_j, BHATTACHARYYA)

        4. 중복 판정:
           만약 (distance < hist_threshold) AND (objects_i ⊆ objects_j):
               fi를 제거 대상으로 표시

        5. 제거 대상 프레임들을 리스트에서 삭제

    다음 반복으로 계속 (총 3회)
```

**Bhattacharyya Distance란?**
- 두 확률 분포(히스토그램)의 유사도를 측정하는 지표
- 값 범위: 0 (완전 동일) ~ 1 (완전 다름)
- 낮을수록 더 유사함

**양방향 윈도우 접근법:**
```
시간 축: ... [f10] [f11] [f12] [f13] [f14] [f15] [f16] ...
                        ↑
                    현재 프레임 f13

윈도우 크기 = 15일 때:
비교 범위: f6 ~ f20 (양쪽으로 ±7 프레임)
```

**반복적 필터링 (3회):**
- **1회차**: 명백히 중복된 프레임 제거
- **2회차**: 업데이트된 컨텍스트로 재평가
- **3회차**: 최종 정제

**예시:**
```
입력 프레임: [100, 105, 110, 250, 255, 400, 650]

1회차 필터링:
  - 프레임 105: 프레임 100과 매우 유사 (distance=0.12) → 제거
  - 프레임 255: 프레임 250과 매우 유사 (distance=0.15) → 제거
  결과: [100, 110, 250, 400, 650]

2회차 필터링:
  - 프레임 110: 프레임 100과 유사 (distance=0.28) → 제거
  결과: [100, 250, 400, 650]

3회차 필터링:
  - 변화 없음 (모두 충분히 다름)
  최종 결과: [100, 250, 400, 650]
```

**주요 파라미터:**
- `hist_threshold`: 0.3 (Bhattacharyya distance 임계값)
- `window_size`: 15 (비교 윈도우 크기, ±7 프레임)
- `profile_iterations`: 3 (필터링 반복 횟수)

**출력**: 시각적으로 구별되는 키프레임 후보

---

#### **단계 3: 재식별 및 프레임 병합 (Re-ID & Frame Merging)**

**목적**: 서로 다른 시간대의 동일 객체를 통합하여 일관된 인스턴스 ID 부여

**문제 상황:**
- 추적 ID는 객체가 화면에서 사라졌다가 다시 나타나면 새로운 ID를 받음
- 예: 쥐 A가 프레임 1-50에서 ID=1, 프레임 200-300에서 ID=7을 받을 수 있음
- 실제로는 같은 쥐지만 추적기는 다른 객체로 인식

**해결책: 외형 기반 재식별**

**알고리즘:**
```
1. 특징 벡터 추출:
   모든 키프레임 후보의 각 객체에 대해:
       - 바운딩 박스 영역 잘라내기
       - 256×128로 리사이즈 (OSNet 표준 입력)
       - ImageNet 통계로 정규화
       - OSNet 모델에 입력
       - 512차원 특징 벡터 추출

2. 유사도 계산:
   모든 객체 쌍 (obj_i, obj_j)에 대해:
       - 코사인 유사도 계산:
         similarity = cos(feature_i, feature_j)
                    = (feature_i · feature_j) / (||feature_i|| × ||feature_j||)

3. 클러스터링:
   - 유사도 > reid_threshold (0.8)인 객체들을 그룹화
   - 각 클러스터 = 하나의 고유한 인스턴스
   - 클러스터에 통합 Instance ID 부여

4. 프레임 병합:
   - 동일한 객체 집합을 가진 프레임들 식별
   - 병합 조건:
     * Instance ID 집합이 동일
     * 외형 유사도 > frame_merge_threshold (0.7)
   - 중복 프레임 제거, 대표 프레임만 유지
```

**코사인 유사도:**
- 값 범위: -1 (완전 반대) ~ 1 (완전 동일)
- 0.8 이상: 매우 유사 → 같은 객체로 판정
- 벡터 간 각도로 유사성을 측정

**예시:**
```
초기 상태:
프레임 100: 객체 [Track_ID=1, Track_ID=2]
프레임 250: 객체 [Track_ID=7, Track_ID=8]
프레임 400: 객체 [Track_ID=12, Track_ID=13]

특징 벡터 추출:
프레임 100, Track_ID=1: feature_vec_1 (512-dim)
프레임 100, Track_ID=2: feature_vec_2 (512-dim)
프레임 250, Track_ID=7: feature_vec_7 (512-dim)
프레임 250, Track_ID=8: feature_vec_8 (512-dim)
프레임 400, Track_ID=12: feature_vec_12 (512-dim)
프레임 400, Track_ID=13: feature_vec_13 (512-dim)

유사도 계산:
cos(vec_1, vec_7) = 0.92  → 같은 객체!
cos(vec_2, vec_8) = 0.88  → 같은 객체!
cos(vec_7, vec_12) = 0.85 → 같은 객체!
cos(vec_8, vec_13) = 0.91 → 같은 객체!

클러스터링 결과:
Cluster A (Instance_ID=1): {Track_1, Track_7, Track_12} → 쥐 A
Cluster B (Instance_ID=2): {Track_2, Track_8, Track_13} → 쥐 B

통합 후:
프레임 100: 객체 [Instance_ID=1, Instance_ID=2]
프레임 250: 객체 [Instance_ID=1, Instance_ID=2]
프레임 400: 객체 [Instance_ID=1, Instance_ID=2]

프레임 병합:
→ 모두 같은 객체 집합 {1, 2}를 가지고 있으므로
→ 프레임 250, 400 제거 (중복)
→ 최종: 프레임 100만 유지
```

**주요 파라미터:**
- `reid_threshold`: 0.8 (객체 매칭 유사도 임계값)
- `frame_merge_threshold`: 0.7 (프레임 병합 유사도 임계값)
- `min_reid_samples`: 2 (클러스터 형성 최소 샘플 수)

**출력**: 통합된 인스턴스 ID를 가진 키프레임 후보

---

#### **단계 4: 탐욕적 커버리지 선택 (Greedy Coverage Selection)**

**목적**: 다양한 객체 상호작용을 커버하는 최소한의 프레임 선택

**핵심 아이디어**:
- 각 프레임이 보여주는 "객체 조합"을 최대한 다양하게 커버
- 적은 프레임으로 많은 정보를 표현

**k-조합 (k-combinations):**
- k=2: 모든 객체 쌍 (상호작용)
- 예: 객체 {1, 2, 3}이 있으면 → 조합 {(1,2), (1,3), (2,3)}

**탐욕 알고리즘:**
```
입력: 키프레임 후보 F = [f1, f2, ..., fn]
      각 프레임 fi의 Instance ID 집합
      k = 조합 크기 (기본값: 2)
      min_new_combos = 최소 새 조합 개수 (기본값: 1)

출력: 선택된 키프레임 리스트 S

1. 초기화:
   covered_combos = 빈 집합
   S = 빈 리스트

2. 각 프레임 fi에 대해 k-조합 생성:
   예: 프레임에 객체 {1, 2, 3, 4}가 있고 k=2면
       combos_i = {(1,2), (1,3), (1,4), (2,3), (2,4), (3,4)}

3. 반복:
   while F가 비지 않음:
       a. 각 프레임 fi의 새로운 조합 개수 계산:
          new_combos_i = combos_i - covered_combos
          new_count_i = |new_combos_i|

       b. 최대 새 조합을 가진 프레임 찾기:
          f_best = argmax(new_count_i)

       c. 종료 조건 확인:
          if new_count_best < min_new_combos:
              break  # 더 이상 유용한 프레임 없음

       d. 프레임 선택:
          S에 f_best 추가
          covered_combos에 new_combos_best 추가
          F에서 f_best 제거

4. return S
```

**상세 예시:**

```
초기 상태:
프레임 100: 객체 {1, 2}          → 조합: {(1,2)}
프레임 250: 객체 {1, 2, 3}       → 조합: {(1,2), (1,3), (2,3)}
프레임 400: 객체 {2, 3, 4}       → 조합: {(2,3), (2,4), (3,4)}
프레임 550: 객체 {1, 4}          → 조합: {(1,4)}
프레임 700: 객체 {1, 2, 3, 4}    → 조합: {(1,2), (1,3), (1,4), (2,3), (2,4), (3,4)}

covered_combos = {}
S = []

【반복 1】
  각 프레임의 새 조합 개수:
    프레임 100: 1개  [(1,2)]
    프레임 250: 3개  [(1,2), (1,3), (2,3)]
    프레임 400: 3개  [(2,3), (2,4), (3,4)]
    프레임 550: 1개  [(1,4)]
    프레임 700: 6개  [(1,2), (1,3), (1,4), (2,3), (2,4), (3,4)]  ← 최대!

  선택: 프레임 700
  S = [700]
  covered_combos = {(1,2), (1,3), (1,4), (2,3), (2,4), (3,4)}

【반복 2】
  각 프레임의 새 조합 개수:
    프레임 100: 0개  (모두 이미 커버됨)
    프레임 250: 0개  (모두 이미 커버됨)
    프레임 400: 0개  (모두 이미 커버됨)
    프레임 550: 0개  (모두 이미 커버됨)

  최대 새 조합 = 0 < min_new_combos (1)
  종료!

최종 선택: S = [700]
```

**다른 예시 (더 복잡한 경우):**
```
프레임 100: {1, 2}        → {(1,2)}
프레임 200: {3, 4}        → {(3,4)}
프레임 300: {5, 6}        → {(5,6)}
프레임 400: {1, 3, 5}     → {(1,3), (1,5), (3,5)}

【반복 1】
  프레임 100: 1개
  프레임 200: 1개
  프레임 300: 1개
  프레임 400: 3개 ← 선택!

  S = [400]
  covered = {(1,3), (1,5), (3,5)}

【반복 2】
  프레임 100: 1개 [(1,2)] ← 새로운 조합!
  프레임 200: 1개 [(3,4)] ← 새로운 조합!
  프레임 300: 1개 [(5,6)] ← 새로운 조합!

  모두 같으므로 첫 번째 선택: 프레임 100
  S = [400, 100]
  covered = {(1,3), (1,5), (3,5), (1,2)}

【반복 3】
  프레임 200: 1개 [(3,4)] ← 선택!
  S = [400, 100, 200]
  covered = {(1,3), (1,5), (3,5), (1,2), (3,4)}

【반복 4】
  프레임 300: 1개 [(5,6)] ← 선택!
  S = [400, 100, 200, 300]
  covered = {(1,3), (1,5), (3,5), (1,2), (3,4), (5,6)}

최종: S = [400, 100, 200, 300] (시간순 정렬: [100, 200, 300, 400])
```

**주요 파라미터:**
- `k`: 2 (조합 크기 - 쌍)
- `min_new_combos`: 1 (최소 새 조합 개수)
- `use_instance_id`: True (Instance ID 사용 여부)

**출력**: 최소 프레임으로 최대 커버리지를 달성한 키프레임 집합

---

#### **단계 4.5: 후처리 프로파일 추적 (Post-Greedy Filtering)** [선택사항]

**목적**: 탐욕 선택 후 남아있을 수 있는 미세한 중복 제거

**알고리즘:**
- 단계 2와 동일한 히스토그램 기반 필터링
- 더 엄격한 임계값 사용 (예: 0.2)
- 단일 반복

**적용 조건:**
- `apply_post_filter=True`로 설정 시

**출력**: 최종 정제된 키프레임 집합

---

#### **단계 5: 결과 저장 및 출력**

**생성되는 출력물:**

1. **키프레임 이미지**
   ```
   4_final_greedy_frames/
   ├── frame_0100.jpg              (원본)
   ├── frame_0100_with_boxes.jpg   (바운딩 박스 포함)
   ├── frame_0250.jpg
   ├── frame_0250_with_boxes.jpg
   └── ...
   ```

2. **JSON 메타데이터** (`keyframe_summary_unified.json`)
   ```json
   {
     "video_info": {
       "name": "video.mp4",
       "total_frames": 5000,
       "fps": 30.0,
       "duration_seconds": 166.67
     },
     "processing_config": {
       "detector": "YOLO",
       "tracker": "ByteTrack",
       "reid_threshold": 0.8,
       "hist_threshold": 0.3,
       "profile_iterations": 3,
       "k_combinations": 2,
       "min_new_combos": 1
     },
     "statistics": {
       "num_keyframes": 12,
       "compression_ratio": 0.0024,
       "primary_selection_count": 45,
       "after_profile_tracking": 28,
       "after_reid": 20,
       "final_greedy_selection": 12
     },
     "keyframes": [
       {
         "frame_index": 100,
         "timestamp_seconds": 3.33,
         "objects": [
           {
             "instance_id": 1,
             "tracking_id": 7,
             "bbox": [120, 80, 200, 160],
             "confidence": 0.92,
             "class": "mouse"
           },
           {
             "instance_id": 2,
             "tracking_id": 8,
             "bbox": [350, 120, 420, 190],
             "confidence": 0.88,
             "class": "mouse"
           }
         ],
         "object_combinations": [[1, 2]],
         "selection_reason": "Covers 1 new combinations"
       }
     ]
   }
   ```

3. **비교 비디오** (선택사항)
   - 원본과 키프레임을 나란히 표시

**출력**: 완전한 키프레임 세트 및 메타데이터

---

## 구성 변형 (Ablation Study)

프로젝트는 각 구성요소의 개별 기여도를 평가하기 위해 **6가지 설정**을 지원합니다:

### 구성 요약표

| 설정명 | ByteTrack | Profile | Re-ID | 목적 |
|--------|-----------|---------|-------|------|
| Full_Model | ✓ | ✓ | ✓ | 완전체 성능 기준 |
| No_ReID | ✓ | ✓ | ✗ | Re-ID 기여도 측정 |
| No_Profile | ✓ | ✗ | ✓ | Profile 기여도 측정 |
| No_ByteTrack | ✗ | ✓ | ✓ | ByteTrack 기여도 측정 |
| Profile_Only | ✗ | ✓ | ✗ | Profile만 사용 |
| Baseline | ✗ | ✗ | ✗ | YOLO만 사용 (기준선) |

### 1. **전체 모델 (Full_Model)**
- 모든 구성요소 활성화
- YOLO + ByteTrack + 프로파일 추적 + Re-ID
- **최고 성능 예상**

### 2. **Re-ID 없음 (No_ReID)**
```python
no_reid=True
```
- Re-ID 비활성화 (reid_threshold=2.0으로 매칭 불가)
- YOLO + ByteTrack + 프로파일 추적
- **Re-ID의 효과 측정**

### 3. **프로파일 없음 (No_Profile)**
```python
profile_iterations=0
apply_post_filter=False
```
- 히스토그램 기반 필터링 비활성화
- YOLO + ByteTrack + Re-ID
- **프로파일 추적의 효과 측정**

### 4. **ByteTrack 없음 (No_ByteTrack)**
```python
profile_reid=True
```
- ByteTrack 추적 비활성화
- YOLO + 프로파일 추적 + Re-ID
- **ByteTrack의 효과 측정**

### 5. **프로파일만 (Profile_Only)**
```python
profile_only=True
```
- ByteTrack과 Re-ID 모두 비활성화
- YOLO + 프로파일 추적만 사용
- **순수 프로파일 추적 성능 측정**

### 6. **베이스라인 (Baseline)**
```python
baseline=True
```
- YOLO 감지만 사용
- ByteTrack, 프로파일 추적, Re-ID 모두 비활성화
- **기준선 성능 측정**

### 기여도 분석 방법

각 컴포넌트의 기여도는 다음과 같이 계산할 수 있습니다:
- **Re-ID 기여도** = Full_Model - No_ReID
- **Profile 기여도** = Full_Model - No_Profile
- **ByteTrack 기여도** = Full_Model - No_ByteTrack

---

## 구체적인 사용 예시

### 예시 1: 단일 비디오 전체 파이프라인 실행

```bash
# 1단계: Ground Truth 생성 (수동 어노테이션)
python create_ground_truth.py --video my_experiment.mp4

# 인터랙티브 GUI가 열림:
# - SPACE: 현재 프레임을 키프레임으로 표시
# - ←/→: 1프레임 이동
# - ↑/↓: 30프레임 점프
# - P: 재생/일시정지
# - D: 마지막 키프레임 삭제
# - S: 저장하고 계속
# - Q/ESC: 저장하고 종료

# 2단계: 모델 실행 및 평가
python evaluate_single_video.py --video my_experiment.mp4

# 출력:
# ✓ 프레임 추출 중... [=============================] 100%
# ✓ 1차 선택 완료: 45개 프레임
# ✓ 프로파일 추적 완료: 28개 프레임
# ✓ Re-ID 및 병합 완료: 20개 프레임
# ✓ 탐욕 선택 완료: 12개 키프레임
#
# 평가 결과:
#   F1-Score (tolerance=0):  0.4500
#   F1-Score (tolerance=15): 0.6800
#   F1-Score (tolerance=30): 0.7500
#   Compression Ratio: 0.0024
#   Runtime: 142.5 seconds
```

### 예시 2: Ablation Study 실행

```bash
python run_ablation_study.py --video my_experiment.mp4

# 6가지 구성을 자동으로 실행:
# [1/6] Full_Model 실행 중...
#   ✓ 키프레임: 12개, F1@30: 0.7500
#
# [2/6] No_ReID 실행 중...
#   ✓ 키프레임: 14개, F1@30: 0.7200
#
# [3/6] No_Profile 실행 중...
#   ✓ 키프레임: 18개, F1@30: 0.6800
#
# [4/6] No_ByteTrack 실행 중...
#   ✓ 키프레임: 15개, F1@30: 0.7100
#
# [5/6] Profile_Only 실행 중...
#   ✓ 키프레임: 16개, F1@30: 0.6500
#
# [6/6] Baseline 실행 중...
#   ✓ 키프레임: 25개, F1@30: 0.5500
#
# 결과 요약:
# Best Configuration: Full_Model (F1=0.7500)
#
# 기여도 분석:
#   Re-ID 기여도: +0.03 (Full_Model - No_ReID)
#   Profile 기여도: +0.07 (Full_Model - No_Profile)
#   ByteTrack 기여도: +0.04 (Full_Model - No_ByteTrack)
```

### 예시 3: 대규모 실험 (20개 비디오)

```bash
python run_experiments.py

# 20개 비디오 × 4개 모델 구성 × 3개 베이스라인 = 140개 실험
# 예상 소요 시간: ~8시간 (GPU 사용 시)
#
# 진행 상황:
# [1/20] akI8YFjEmUw.mp4
#   [1/4] YOLO_BOTSORT: F1@30=0.68, Runtime=156s
#   [2/4] YOLO_ByteTrack: F1@30=0.72, Runtime=148s
#   [3/4] RTDETR_BOTSORT: F1@30=0.65, Runtime=178s
#   [4/4] RTDETR_ByteTrack: F1@30=0.70, Runtime=165s
# ...
#
# 최종 결과: experiment_results/20250117_123456/
```

---

## 평가 방법

### 1. Ground Truth 생성

**방법**: TvSum 데이터셋 기반 수동 어노테이션

**프로세스:**
1. 비디오의 중요도 점수 분석 (TvSum annotations)
2. 중요도 피크 프레임 식별
3. 동일 장면 내 중복 프레임 제거
4. 수동 정제 및 품질 검증
5. 객체 구성 변화에 초점

**품질 기준:**
- 객체 수 변화 순간 포착
- 새로운 상호작용 시작점
- 장면 전환점
- 시각적 다양성
- 불필요한 중복 최소화

**출력 형식:**
```json
{
  "video_name": "my_experiment.mp4",
  "total_frames": 5000,
  "fps": 30.0,
  "keyframes": [150, 570, 810, 1200, 1350, 1680, 2100],
  "num_keyframes": 7,
  "compression_ratio": 0.0014,
  "annotation_date": "2025-01-17",
  "annotator": "researcher_1"
}
```

### 2. 매칭 알고리즘

**시간적 허용 오차를 고려한 일대일 매칭:**

```python
def match_keyframes(predicted, ground_truth, tolerance):
    """
    predicted: 예측된 키프레임 인덱스 리스트
    ground_truth: Ground Truth 키프레임 인덱스 리스트
    tolerance: 허용 오차 (프레임 단위)

    Returns: (true_positives, false_positives, false_negatives)
    """
    matched_gt = set()
    matched_pred = set()

    # 정렬하여 일관성 보장
    pred_sorted = sorted(set(predicted))
    gt_sorted = sorted(set(ground_truth))

    # 각 예측 프레임에 대해 가장 가까운 GT 찾기
    for pred_frame in pred_sorted:
        best_gt = None
        best_distance = float('inf')

        for gt_frame in gt_sorted:
            if gt_frame in matched_gt:
                continue  # 이미 매칭됨

            distance = abs(pred_frame - gt_frame)

            # 허용 오차 내에서 가장 가까운 것 선택
            if distance <= tolerance and distance < best_distance:
                best_gt = gt_frame
                best_distance = distance

        # 매칭 성공
        if best_gt is not None:
            matched_gt.add(best_gt)
            matched_pred.add(pred_frame)

    tp = len(matched_pred)           # True Positives
    fp = len(pred_sorted) - tp       # False Positives
    fn = len(gt_sorted) - tp         # False Negatives

    return tp, fp, fn
```

**매칭 예시:**

```
Ground Truth: [100, 500, 1000, 1500]
Predicted:    [105, 490, 1020, 1800]
Tolerance:    15 프레임 (±0.5초 @ 30fps)

매칭 과정:
  예측 105 vs GT 100: |105-100|=5 ≤ 15 ✓ → 매칭!
  예측 490 vs GT 500: |490-500|=10 ≤ 15 ✓ → 매칭!
  예측 1020 vs GT 1000: |1020-1000|=20 > 15 ✗
  예측 1020 vs GT 1500: |1020-1500|=480 > 15 ✗
  예측 1800 vs GT 1500: |1800-1500|=300 > 15 ✗

결과:
  TP = 2 (105, 490 매칭됨)
  FP = 2 (1020, 1800 매칭 실패)
  FN = 2 (GT 1000, 1500 놓침)
```

---

## 평가 Metrics

### 1. F1-Score (시간적 허용 오차 포함)

**정의:**
```
Precision = TP / (TP + FP)
Recall = TP / (TP + FN)
F1-Score = 2 × (Precision × Recall) / (Precision + Recall)

여기서:
  TP (True Positive): Ground Truth와 매칭된 예측 프레임 수
  FP (False Positive): 매칭되지 않은 예측 프레임 수
  FN (False Negative): 놓친 Ground Truth 프레임 수
```

**허용 오차 수준:**
- **tolerance=0**: 정확히 일치해야 함 (가장 엄격)
- **tolerance=15**: ±0.5초 허용 (30fps 기준)
- **tolerance=30**: ±1초 허용 (가장 관대)

**해석:**
- **Precision**: 예측한 키프레임 중 실제로 중요한 비율
  - 높음 → 불필요한 프레임 적음
- **Recall**: 실제 중요한 프레임 중 찾아낸 비율
  - 높음 → 중요한 순간 놓치지 않음
- **F1-Score**: Precision과 Recall의 조화 평균
  - 높음 → 균형 잡힌 성능

**계산 예시:**
```
Ground Truth: 10개 키프레임
Predicted: 15개 키프레임
Tolerance: 15 프레임

매칭 결과:
  TP = 8  (8개 예측이 GT와 매칭)
  FP = 7  (15-8 = 7개가 불필요)
  FN = 2  (10-8 = 2개 GT를 놓침)

계산:
  Precision = 8 / (8+7) = 8/15 = 0.5333 (53.33%)
  Recall = 8 / (8+2) = 8/10 = 0.8000 (80.00%)
  F1-Score = 2 × (0.5333 × 0.8) / (0.5333 + 0.8)
           = 2 × 0.4266 / 1.3333
           = 0.6400 (64.00%)

해석:
  - 예측의 53%가 정확 (약간 과추출)
  - GT의 80%를 성공적으로 찾음 (좋은 커버리지)
  - 전체 성능: 64% (양호)
```

### 2. Compression Ratio (압축률)

**정의:**
```
Compression Ratio = num_keyframes / total_frames
```

**의미:**
- 전체 프레임 중 키프레임의 비율
- **낮을수록 좋음** (더 효율적인 압축)

**예시:**
```
비디오: 5000 프레임 (30fps, 166초)
키프레임: 12개

Compression Ratio = 12 / 5000 = 0.0024 (0.24%)

해석:
  - 원본의 0.24%만 유지
  - 417배 압축 (1/0.0024)
  - 166초 비디오 → 12개 이미지로 요약
```

**일반적인 범위:**
- **< 0.01 (1%)**: 매우 효율적
- **0.01 ~ 0.05 (1-5%)**: 적절함
- **> 0.10 (10%)**: 과도한 추출

### 3. Runtime (실행 시간)

**측정 단위**: 초 (seconds)

**포함 단계:**
1. 비디오 로딩 및 프레임 추출
2. 객체 감지 (YOLO/RT-DETR)
3. 객체 추적 (ByteTrack/BOT-SORT)
4. 프로파일 추적 (히스토그램 계산 및 비교)
5. Re-ID 특징 추출
6. 탐욕 알고리즘 실행
7. 결과 저장

**일반적인 성능:**
```
GPU (NVIDIA RTX 3090):
  - 짧은 비디오 (< 3000 프레임): 60-120초
  - 중간 비디오 (3000-6000 프레임): 120-240초
  - 긴 비디오 (> 6000 프레임): 240-400초

CPU (Intel i7):
  - 2-3배 더 오래 걸림
```

### 4. 베이스라인 비교: Uniform Sampling

**방법**: 일정 간격으로 프레임 샘플링

**설정:**
- **Uniform_10**: 10프레임마다 1개 추출
- **Uniform_30**: 30프레임마다 1개 추출 (매 1초)
- **Uniform_60**: 60프레임마다 1개 추출 (매 2초)

**비교 결과 예시:**
```
비디오: 5000 프레임

| 방법            | 키프레임 | F1@0  | F1@15 | F1@30 | 압축률  |
|-----------------|----------|-------|-------|-------|---------|
| KABS (Full)     | 12       | 0.45  | 0.68  | 0.75  | 0.0024  |
| KABS (No Prof)  | 18       | 0.42  | 0.62  | 0.68  | 0.0036  |
| Uniform_30      | 167      | 0.25  | 0.38  | 0.42  | 0.0334  |
| Uniform_60      | 83       | 0.18  | 0.30  | 0.35  | 0.0166  |

분석:
  - KABS는 Uniform_30보다 14배 적은 프레임으로
  - 78% 더 높은 F1-Score 달성 (0.75 vs 0.42)
  - 월등히 우수한 효율성
```

### 5. 모델 비교 메트릭

**프레임 중복도 (Frame Overlap):**
```
두 모델 A, B의 키프레임 집합:
  A = {100, 200, 300}
  B = {105, 200, 400}

Jaccard Index (tolerance=15):
  매칭: {200}과 {200}, {100}과 {105}
  intersection = 2
  union = 4 (총 고유 키프레임)
  Jaccard = 2/4 = 0.50

해석: 50% 일치
```

**모델 유사도 매트릭스:**
```
          YOLO_BT  YOLO_BS  RTDETR_BT  RTDETR_BS
YOLO_BT      1.00     0.85      0.72       0.68
YOLO_BS      0.85     1.00      0.70       0.75
RTDETR_BT    0.72     0.70      1.00       0.88
RTDETR_BS    0.68     0.75      0.88       1.00

해석:
  - YOLO_BT와 YOLO_BS: 85% 유사 (추적기 영향 적음)
  - RTDETR_BT와 RTDETR_BS: 88% 유사
  - YOLO vs RTDETR: 68-75% 유사 (검출기 영향 큼)
```

---

## 성능 기대치 및 벤치마크

### 좋은 결과의 기준

**F1-Score:**
- F1@30 > 0.60: 양호 (60% 이상 정확도, 1초 허용)
- F1@15 > 0.50: 양호 (50% 이상 정확도, 0.5초 허용)
- F1@0 > 0.30: 양호 (30% 이상 정확도, 정확히 일치)

**Compression Ratio:**
- < 0.05: 우수 (5% 미만)
- < 0.10: 양호 (10% 미만)
- > 0.20: 개선 필요 (너무 많은 프레임)

**Runtime:**
- < 3분/비디오: 우수
- < 5분/비디오: 양호
- > 10분/비디오: 최적화 필요

### 베이스라인 대비 향상도

**일반적인 성능:**
- Uniform Sampling F1@30: 0.30 ~ 0.45
- KABS F1@30: 0.60 ~ 0.75
- **향상도: 33% ~ 67%**

**압축 효율:**
- Uniform_30: 압축률 ~3.3%
- KABS: 압축률 ~0.2% ~ 0.5%
- **6배 ~ 16배 더 효율적**

---

## 최근 개선 사항

프로젝트의 최근 업데이트 내역:

1. **Re-ID 임계값 조정**
   - Profile_Only 및 No_Tracking 모드에서 더 낮은 임계값 사용
   - 프레임 병합 성능 향상

2. **범례 형식 업데이트**
   - 프레임 번호 기반 표시
   - 프로파일 용어 사용

3. **4번째 Ablation 구성 추가**
   - 더 포괄적인 구성요소 분석

4. **시각화 정리**
   - 논문용 깔끔한 출력
   - 불필요한 메트릭 제거
   - 단순화된 범례

---

## 프로젝트 구조

```
KABS_paper/
├── 주요 알고리즘
│   ├── yolo_osnet_4_with_filtering_updated (1).py  (메인 파이프라인, 1616줄)
│   ├── model_wrapper.py                             (모델 래퍼)
│   └── baseline_methods.py                          (베이스라인 구현)
│
├── 평가 시스템
│   ├── evaluation_metrics.py                        (메트릭 계산)
│   ├── evaluate_single_video.py                     (단일 비디오 평가)
│   ├── run_experiments.py                           (전체 실험 실행)
│   └── run_ablation_study.py                        (Ablation Study)
│
├── Ablation 변형
│   ├── keyframe_extraction_profile_only.py          (프로파일만)
│   ├── keyframe_extraction_profile_reid.py          (프로파일+Re-ID)
│   ├── keyframe_extraction_reid_only.py             (Re-ID만)
│   └── keyframe_extraction_baseline.py              (베이스라인 - YOLO만)
│
├── 유틸리티
│   ├── create_ground_truth.py                       (GT 어노테이션 도구)
│   └── compare_model_frames.py                      (모델 비교)
│
├── 데이터셋
│   └── Keyframe-extraction/Dataset/
│       ├── Videos/                                  (20개 테스트 비디오)
│       └── Keyframe/                                (Ground Truth)
│
├── 사전 학습 모델
│   └── osnet_x1_0_market_256x128_amsgrad_ep150_stp60_lr0.0015_b64_fb10_softmax_labelsmooth_flip.pth
│
└── 테스트 비디오
    ├── v_1.mp4
    ├── v_2.mp4
    ├── v_3.mp4
    └── v_4.mp4
```

---

## 결론

KABS는 다음과 같은 특징을 가진 최첨단 키프레임 추출 시스템입니다:

### 핵심 강점
1. **지능형 다단계 파이프라인**: 각 단계가 특정 문제 해결
2. **객체 중심 접근**: 추적 + 재식별로 의미 있는 구성 변화 포착
3. **효율적 압축**: 0.2-0.5% 압축률로 높은 정보 유지
4. **우수한 성능**: 베이스라인 대비 33-67% F1-Score 향상
5. **포괄적 평가**: Ground Truth 기반 정량적 평가

### 혁신적 요소
- **결합 히스토그램**: Brightness + Saturation으로 강건한 유사도 측정
- **양방향 윈도우**: 시간적 컨텍스트를 고려한 필터링
- **탐욕적 커버리지**: 조합론 기반 최적화로 최소 프레임 선택
- **반복적 정제**: 다단계 필터링으로 점진적 개선

### 응용 분야
- 행동 분석 연구 (동물 실험)
- 비디오 요약 및 인덱싱
- 감시 시스템 (이벤트 탐지)
- 스포츠 분석 (하이라이트 추출)
- 의료 영상 (수술 비디오 요약)

KABS는 단순한 균등 샘플링을 넘어, **의미론적으로 중요한 순간**을 지능적으로 식별하는 시스템으로, 비디오 분석 연구에 큰 기여를 합니다.
